import React from "react";
import styles from "./styles.module.scss";

const BackgroundBlueHazeElement: React.FC = () => {
  return <span className={`${styles.element} ${styles.blueHaze}`}></span>;
};

export default BackgroundBlueHazeElement;
