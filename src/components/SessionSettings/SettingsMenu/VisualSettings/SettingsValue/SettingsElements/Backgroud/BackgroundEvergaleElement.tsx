import React from "react";
import styles from "./styles.module.scss";

const BackgroundEvergladeElement: React.FC = () => {
  return <span className={`${styles.element} ${styles.everglade}`}></span>;
};

export default BackgroundEvergladeElement;
