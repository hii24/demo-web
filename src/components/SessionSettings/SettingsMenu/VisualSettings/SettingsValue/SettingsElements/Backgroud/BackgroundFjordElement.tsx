import React from "react";
import styles from "./styles.module.scss";

const BackgroundFjordElement: React.FC = () => {
  return <span className={`${styles.element} ${styles.fjord}`}></span>;
};

export default BackgroundFjordElement;
