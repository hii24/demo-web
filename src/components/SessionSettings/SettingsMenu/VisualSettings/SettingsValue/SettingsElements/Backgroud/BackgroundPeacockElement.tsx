import React from "react";
import styles from "./styles.module.scss";

const BackgroundPeacockElement: React.FC = () => {
  return <span className={`${styles.element} ${styles.peacock}`}></span>;
};

export default BackgroundPeacockElement;
