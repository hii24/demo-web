import React from "react";
import styles from "./styles.module.scss";

const BackgroundRiverElement: React.FC = () => {
  return <span className={`${styles.element} ${styles.river}`}></span>;
};

export default BackgroundRiverElement;
