import React from "react";
import styles from "./styles.module.scss";

const BackgroundValleyElement: React.FC = () => {
  return <span className={`${styles.element} ${styles.valley}`}></span>;
};

export default BackgroundValleyElement;
